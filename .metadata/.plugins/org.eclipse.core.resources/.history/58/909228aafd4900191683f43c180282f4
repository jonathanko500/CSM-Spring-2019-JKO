import java.util.*;

public class test {

	public static void main(String[] args)
	{
		Queue<Event> prac = new PriorityQueue<Event>();
		Event start = new ArriveEvent(5,4);
		prac.add(start);
		Object[] list = prac.toArray();
		for(int i=0;i<list.length;i++)
		{
			System.out.println(start.getTime());
		}
	}

}
